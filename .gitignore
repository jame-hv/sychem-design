# Claude Code local settings (machine-specific, not for sharing)
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db

# Original .dc.html design-tool bundles — kept locally as a backup only.
# Already extracted into the static pages/assets above; not needed to deploy.
source-dc/
